version: "3.8"

services:
  textboard:
    build: .
    container_name: textboard
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_PATH=/app/data/textboard.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
